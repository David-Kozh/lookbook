import collection from "./collection";
import collection2 from "./collection2";

//** Example data. This would be returned by firestore
//* Used by ProfileSection to display the example collection names.
const exampleCollections = [
    collection,
    collection2
]

export default exampleCollections;